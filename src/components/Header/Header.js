import React from 'react';
import PropTypes from 'prop-types';
import './Header.css';

const Header = () => (
  <div className="Header">
    <h1 className="logo">
      OFF MENU STATISTICS
    </h1>
  </div>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
